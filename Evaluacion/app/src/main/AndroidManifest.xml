<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="epn.moviles.examenapp">

    <uses-permission android:name="android.permission.INTERNET"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name=".ListaPacienteActivity">
        </activity>
        <activity android:name=".CrearPacienteActivity">
        </activity>
        <activity android:name=".ListaHijos2Activity">
        </activity>
        <activity android:name=".EPacienteActivity">
        </activity>
        <activity android:name=".ListaMedicamentosActivity">
        </activity>
        <activity android:name=".CrearMedicamenteActivity">
        </activity>
    </application>

</manifest>